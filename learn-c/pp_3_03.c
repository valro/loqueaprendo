/*
 *Books are identified by an International Standard Book Number (ISBN). ISBNs assignied after Jannuary 1, 2007 contains 13 digits; arranged in five groups, such as 978-0-393-97950-3 (older ISBDs use 10 digits). The first group (the GSI prefix) is currently either 978 or 979. The group identifier specifies the language or country of origin (for example 0 and 1 are used in English-speaking countries). The publisher code identifies the the publisher (393 is the code for W.W. Norton). The item number is assigned by the publisher to identify a specific book (97950 is the code for this book). An ISBN ends with a check digit that's used to verify the accuracy of the preceding digits. Write a program that breaks down an ISBN entered by the user.
 *
